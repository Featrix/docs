API
===

.. toctree::

.. autosummary::

The API is accessed via this single class, `Featrix`.

There are two layers to the API, one prefixed with `EZ_` that is a simple to use interface, and a second which is a lower-level interface.

These will be split into two modules in coming releases for clarity.

.. autoclass::  featrixclient.networkclient.Featrix
    :members:


